version: '3.2'

services:
  olmarket:
    image: registry.ortolang.fr/ortolang/ortolang-market:master
    environment: 
      - 'api_url=https://repo-dev.ortolang.fr/api'
      - 'prerender_server_ip=prerender'
      - 'prerender_server_port=3000'
    deploy:
      labels:
        traefik.backend: "www-dev"
        traefik.frontend.rule: "Host:www-dev.ortolang.fr"
        traefik.docker.network: "proxy"
        traefik.enable: "true"
        traefik.port: "80"
        traefik.default.protocol: "http"
        traefik.frontend.entryPoints: "http,https"
    networks:
      - proxy
      - ortolang-vdorto

networks:
  ortolang-vdorto:
    external: true
  proxy:
    external: true