<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <groupId>fr.ortolang</groupId>
    <artifactId>market</artifactId>
    <name>Ortolang Diffusion Marketplace Application</name>
    <version>4.1.2-SNAPSHOT</version>
    <packaging>war</packaging>
    <url>http://www.ortolang.fr</url>

    <parent>
        <groupId>fr.ortolang</groupId>
        <artifactId>ortolang</artifactId>
        <version>4</version>
    </parent>

    <scm>
        <connection>scm:git:git@forge.ortolang.fr:ortolang-market.git</connection>
        <developerConnection>scm:git:git@forge.ortolang.fr:ortolang-market.git</developerConnection>
        <url>http://git.ortolang.fr/ortolang-market.git/</url>
        <tag>HEAD</tag>
    </scm>

    <properties>
        <version.grunt.maven.plugin>1.5.0</version.grunt.maven.plugin>
        <version.clean.maven.plugin>2.6.1</version.clean.maven.plugin>
        <version.sonar.maven.plugin>2.5</version.sonar.maven.plugin>
        <sonar.language>js</sonar.language>
        <sonar.sources>src/main/webapp/static/app</sonar.sources>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
    </properties>

    <repositories>
        <repository>
            <id>ortolang-maven-repository</id>
            <url>http://maven.ortolang.fr/public/</url>
            <releases>
                <enabled>true</enabled>
            </releases>
            <snapshots>
                <enabled>true</enabled>
            </snapshots>
        </repository>
    </repositories>

    <pluginRepositories>
        <pluginRepository>
            <id>ortolang-maven-repository</id>
            <url>http://maven.ortolang.fr/public/</url>
            <releases>
                <enabled>true</enabled>
            </releases>
            <snapshots>
                <enabled>true</enabled>
            </snapshots>
        </pluginRepository>
    </pluginRepositories>

    <build>
        <finalName>ortolang-market</finalName>
        <plugins>
            <plugin>
                <groupId>pl.allegro</groupId>
                <artifactId>grunt-maven-plugin</artifactId>
                <version>${version.grunt.maven.plugin}</version>
                <configuration>
                    <!-- relative to src/main/webapp/, default: static -->
                    <jsSourceDirectory>static</jsSourceDirectory>

                    <!-- example options usage to get verbose output in logs -->
                    <gruntOptions>
                        <gruntOption>--verbose</gruntOption>
                    </gruntOptions>

                    <!-- example npm install env variable -->
                    <!--
                    <npmEnvironmentVar>
                        <PHANTOMJS_CDNURL>http://cnpmjs.org/downloads</PHANTOMJS_CDNURL>
                    </npmEnvironmentVar>
                    -->

                    <!-- example options usage to filter variables in given resource -->
                    <!--
                    <filteredResources>
                        <filteredResource>maven-properties.json</filteredResource>
                    </filteredResources>
                    -->
                </configuration>
                <executions>
                    <execution>
                        <goals>
                            <goal>create-resources</goal>
                            <goal>npm</goal>
                            <goal>bower</goal>
                            <goal>grunt</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
            <plugin>
                <artifactId>maven-war-plugin</artifactId>
                <version>2.3</version>
                <configuration>
                    <warSourceExcludes>static/**</warSourceExcludes>
                    <webResources>
                        <resource>
                            <!-- this is relative to the pom.xml directory -->
                            <directory>target-grunt/dist</directory>
                        </resource>
                    </webResources>
                    <archive>
                        <manifest>
                            <addDefaultImplementationEntries>true</addDefaultImplementationEntries>
                            <addDefaultSpecificationEntries>true</addDefaultSpecificationEntries>
                        </manifest>
                        <manifestEntries>
                            <Artefact-Version>${project.version}</Artefact-Version>
                        </manifestEntries>
                    </archive>
                </configuration>
            </plugin>
            <plugin>
		<artifactId>maven-assembly-plugin</artifactId>
                <version>2.6</version>
                <configuration>
                    <descriptors>
                        <descriptor>src/main/assembly/static.xml</descriptor>
                    </descriptors>
                </configuration>
		<executions>
	            <execution>
          	        <id>make-assembly</id> <!-- this is used for inheritance merges -->
	                <phase>package</phase> <!-- bind to the packaging phase -->
	                <goals>
		           <goal>single</goal>
                        </goals>
                    </execution>
                </executions>
 	    </plugin>
            <plugin>
                <groupId>org.codehaus.mojo</groupId>
                <artifactId>sonar-maven-plugin</artifactId>
                <version>${version.sonar.maven.plugin}</version>
            </plugin>
            <plugin>
                <artifactId>maven-clean-plugin</artifactId>
                <version>${version.clean.maven.plugin}</version>
                <configuration>
                    <filesets>
                        <fileset>
                            <directory>${basedir}</directory>
                            <includes>
                                <include>target-grunt/**/*</include>
                                <include>target-grunt</include>
                            </includes>
                            <followSymlinks>false</followSymlinks>
                        </fileset>
                    </filesets>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.codehaus.mojo</groupId>
                <artifactId>license-maven-plugin</artifactId>
                <version>1.8</version>
                <configuration>
                    <licenseName>lgpl_v3</licenseName>
                    <strictChecking>true</strictChecking>
                    <encoding>UTF-8</encoding>
                    <offline>true</offline>
                </configuration>
                <executions>
                    <execution>
                        <id>download-licenses</id>
                        <goals>
                            <goal>download-licenses</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>

</project>
